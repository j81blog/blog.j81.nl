# Hugo build output
public/
resources/_gen/
.hugo_build.lock

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Environment / secrets
.env
.env.*

# Node (if npm tools are ever added)
node_modules/

# Temp files
*.tmp
*.log

# Working data is local only — WordPress export, full backup, color palette source
# These files are large and contain personal data; keep them off the repo
working_data/

# WordPress media files are committed to the repo (static/wp-content/)
# ~196 MB — within GitHub's limits and required for GitHub Pages deployment
