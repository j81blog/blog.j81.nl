# Hugo build output
public/
resources/_gen/
.hugo_build.lock

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Environment / secrets
.env
.env.*

# Node (if npm tools are ever added)
node_modules/

# Temp files
*.tmp
*.log

# Working data is local only — WordPress export, full backup, color palette source
# These files are large and contain personal data; keep them off the repo
working_data/

# WordPress media files are served from static/wp-content/uploads/
# They total ~196 MB and are not committed — copy from the backup when needed:
#   working_data\2026-05-19_14-41-51_D2X4LZ6706_FILE-DBDUMP-WPPLUGIN\wp-content\uploads
static/wp-content/
